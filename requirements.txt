# Core training dependencies
torch>=2.0.0
torchvision>=0.15.0
h5py>=3.8.0
numpy>=1.24.0
tensorboard>=2.13.0
Pillow>=9.5.0
matplotlib>=3.7.0

# OpenVLA dependencies
transformers>=4.40.0
timm>=0.9.0
tokenizers>=0.15.0

# Optional: 4-bit quantisation for single-GPU systems
# bitsandbytes>=0.41.0

# Optional: faster attention (requires CUDA 11.6+)
# flash-attn>=2.0.0

# Stage 0 fine-tuning (LoRA)
peft>=0.10.0
accelerate>=0.27.0
